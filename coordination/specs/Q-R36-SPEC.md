# Q-R36-SPEC.md — Phase 2 Close-Walk (Round 36, Audit Tier)

_Audit tier: Implementer wears both Architect and Implementer hats. This file IS the spec; no separate SPEC-AUDIT.md._

---

## 1. Goal

Close Phase 2 of the Tessera project. After chore-B this round, the project enters HARD STOP — no Phase 3 work without separate operator authorization. Eight deliverables:

1. `coordination/PHASE-2-CLOSE-WALK.md` — primary close-walk document
2. R32 carry-forward closures (SCOPING-MEMO surgery, 4 weak ACs, execSync, R26 MINOR-2, Q-R26-SPEC disambiguation, q28 MINOR-3)
3. R34 carry-forward closures (LS-3/LS-4 spec amendments, SPEC-AUTHORING-CHECKLIST.md, reinforcement appends)
4. Subprocess-hang Tessera-local fixes (q29/q34 skip guards + grep audit + CLAUDE-ARCHITECT.md note)
5. MR-2 CLAUDE-IMPLEMENTER.md 3-pass consolidation
6. PR-F7 hybrid Reviewer audit (Reviewer stage — this spec records what the Reviewer will check)
7. `coordination/ANCHOR-BACKFLOW-2026-05-18.md` — operator-scheduled PR content
8. Rule 6 canonical landing in `~/.claude/CROSS-PROJECT-MEMORIAL.md` (Memorial-Updater stage)

---

## 2. Mechanism

### 2.1 Brainstorm (3 approaches, required discipline)

**Approach A (SELECTED):** Code-changes first, documentation deliverables last. Rationale: front-loads binding-command verification; PHASE-2-CLOSE-WALK.md and ANCHOR-BACKFLOW-2026-05-18.md depend on final clean state and are best written after all fixes land.

**Approach B:** Documentation first, then code. Rejected: Cannot finalize close-walk docs until test suite is clean.

**Approach C:** Interleave code and docs per deliverable order. Rejected: Creates intermediate dirty states that complicate per-step attestation.

### 2.2 Design (component boundaries, required discipline)

**Created:** `test/q36-phase2-close-walk.test.ts`, `coordination/PHASE-2-CLOSE-WALK.md`, `coordination/ANCHOR-BACKFLOW-2026-05-18.md`, `coordination/SPEC-AUTHORING-CHECKLIST.md`, `coordination/specs/Q-R36-SPEC.md`

Note: `.js` files are gitignored (generated by tsc from `.ts` source); git diff shows only `.ts` changes.

**Modified (pre-authorized at file granularity per wu-07 scope):**
- `test/q29-k8s-adapter.test.ts` — skip guard + AC-R29-13 forward-protection SHA pin
- `test/q34-event-conditional-attribution.test.ts` — skip guard + AC-R34-19 STAGED file path + `\Z` → `$` fix
- `test/q32-slice3-close-walk.test.ts` — AC-R32-18 + 4 weak ACs + AC-R32-20 SHA pin
- `test/q25-l0-contract.test.ts` — execSync → execFileSync
- `test/q30-nvlink-adapter.test.ts` — execSync → execFileSync
- `test/q28-slurm-adapter.test.ts` — snap2 source_id/source_version assertions
- `engine/topology/common-mode-attribution.ts` — R26 MINOR-2 per-distinct-shard dedup
- `coordination/SCOPING-MEMO-v0.3.md` — MAJOR-1 surgery
- `coordination/specs/Q-R32-SPEC.md` — 4 weak AC amendments
- `coordination/specs/Q-R26-SPEC.md` — AC-R26-14 disambiguation
- `coordination/specs/Q-R34-SPEC.md` — LS-3 window boundary + LS-4 `\Z` fix
- `CLAUDE-ARCHITECT.md` — R34 reinforcement appends (3 entries) + subprocess note
- `CLAUDE-IMPLEMENTER.md` — R34 reinforcement appends (3 entries) + MR-2 consolidation
- `CLAUDE-COMMON.md` — MR-2 Pass 3 universal pattern promotions

**Integration points:**
- Subprocess skip guard: `if (process.env.NODE_TEST_CONTEXT || process.env.NODE_TEST_WORKER_ID) { t.skip('subprocess-spawn skipped in worker context'); return; }` at top of AC-R29-12 and AC-R34-21 test bodies; does not affect standalone runs
- execFileSync migration: swap `execSync('git diff ...')` string form → `execFileSync('git', ['diff', ...], {encoding:'utf8'})` array form; no behavior change
- R26 MINOR-2: common-mode-attribution.ts loop restructured to collect distinct member_shards before aggregating event_ts; callers unchanged (return type identical); docstring updated to match
- MR-2 consolidation: 3 commits (Pass 1 cross-project de-dup → Pass 2 thematic composite → Pass 3 CLAUDE-COMMON promotions); self-application gate at chore-A per Rule 5
- SCOPING-MEMO surgery: move `### Vendor fungibility` heading from inside A12–A17 bullet list (line 267) to a new sub-section after A17; restore A14 rationale to the A14 bullet

**Failure modes:**
- MR-2 self-application gate: if consolidation removes trigger conditions → HALT per halt condition 3
- SCOPING-MEMO surgery: heading insertion re-corrupts list if wrong location → verify 30-line window post-edit per REINFORCED 2026-05-18 (R32 MAJOR-1)
- Subprocess skip guard: if NODE_TEST_CONTEXT semantics change in future Node.js → documented in ANCHOR-BACKFLOW as PR candidate

### 2.3 Execution order

1. RED commit — failing stubs for `test/q36-phase2-close-walk.test.ts`
2. Deliverable 4: q29/q34 skip guards + `\Z` fix + grep audit + CLAUDE-ARCHITECT.md note
3. Deliverable 2: SCOPING-MEMO surgery + q32 AC fixes + execSync conversions + R26 MINOR-2 + Q-R26-SPEC + q28 snap2
4. Deliverable 3: Q-R34-SPEC amendments (LS-3/LS-4) + SPEC-AUTHORING-CHECKLIST.md + CLAUDE-ARCHITECT.md/CLAUDE-IMPLEMENTER.md R34 reinforcement appends
5. Deliverable 5: MR-2 Pass 1 → Pass 2 → Pass 3 (3 sequential commits)
6. Deliverable 1: `coordination/PHASE-2-CLOSE-WALK.md`
7. Deliverable 7: `coordination/ANCHOR-BACKFLOW-2026-05-18.md`
8. Chore-A: coordination artifacts (this spec + MEMORIAL.md + NEXT-ROLE.md)
9. Deliverable 6: PR-F7 hybrid Reviewer audit (Reviewer stage)
10. Deliverable 8: Rule 6 canonical landing (Memorial-Updater stage)
11. Chore-B: forward protection runtime test

---

## 3. Acceptance Criteria

### Deliverable 4 — Subprocess-hang fixes

**AC-R36-1** (q29 skip guard)
Given `test/q29-k8s-adapter.test.ts` AC-R29-12 test body (the `execFileSync('node', '--test', ...)` invocation),
when `process.env.NODE_TEST_CONTEXT` or `process.env.NODE_TEST_WORKER_ID` is set (indicating worker context),
then the test calls `t.skip(...)` and returns without spawning the child process.

**AC-R36-2** (q34 skip guard)
Given `test/q34-event-conditional-attribution.test.ts` AC-R34-21 test body (the subprocess-spawn invocation),
when `process.env.NODE_TEST_CONTEXT` or `process.env.NODE_TEST_WORKER_ID` is set,
then the test calls `t.skip(...)` and returns without spawning the child process.

**AC-R36-3** (grep audit — no other subprocess-spawning test files)
Given the grep result of `execFileSync.*node.*--test` across all `test/q*.test.ts` files (excluding q29 and q34),
when the command is run at chore-A SHA,
then the grep returns 0 matches (only q29 and q34 carry this pattern).

**AC-R36-4** (forward-protection tests stabilized)
Given the three currently-failing forward-protection tests (q29 AC-R29-13, q32 AC-R32-20, q34 AC-R34-19),
when each is read after this round's edits,
then: AC-R29-13 uses pinned SHAs (`778cff8..<R29-chore-B-SHA>`); AC-R32-20 uses pinned SHAs (`6466940..7f737d6`); AC-R34-19's allowed-set includes the operator-added STAGED file path; all three PASS when run standalone.

### Deliverable 2 — R32 carry-forwards

**AC-R36-5** (SCOPING-MEMO MAJOR-1 surgery)
Given `coordination/SCOPING-MEMO-v0.3.md` at the A12–A17 bullet list section,
when a 30-line window around the former heading insertion point (line ~267) is read,
then: A14's full rationale paragraph is contiguous with its `- **A14:** …` bullet; no h3 heading appears between A14 and A15; `### Vendor fungibility` appears as a subsection AFTER the A17 bullet; and the orphaned rationale sentence is removed from the TAGGED-FUTURE vendor paragraph.

**AC-R36-6** (AC-R32-2 strengthened — placement-aware)
Given `test/q32-slice3-close-walk.test.ts` AC-R32-2 test body,
when SCOPING-MEMO-v0.3.md has `### Vendor fungibility` inside the A12–A17 list (pre-surgery state),
then the assertion FAILS (detects the structural defect). When the heading is after A17 (post-surgery), the assertion PASSES. The assertion checks that `### Vendor fungibility` appears AFTER `- **A17` in the file content, not merely that the string exists.

**AC-R36-7** (AC-R32-7 strengthened — literal gauge + degraded)
Given `test/q32-slice3-close-walk.test.ts` AC-R32-7 test body,
when read at the assertion block,
then it checks for both the literal string `'gauge'` AND either `'missed_scrape'` or `'degraded'` in the relevant test file section; NOT only `includes('MINOR-3')`.

**AC-R36-8** (AC-R32-13 strengthened — REVIEWER_REPORT_REGEX identifier + call)
Given `test/q32-slice3-close-walk.test.ts` AC-R32-13 test body,
when read at the assertion block,
then it checks for `REVIEWER_REPORT_REGEX` identifier AND a `.test(` call against it; NOT only `includes('REVIEWER-REPORT')`.

**AC-R36-9** (AC-R32-14 strengthened — § 3.2 comment adjacent to env: subEnv)
Given `test/q32-slice3-close-walk.test.ts` AC-R32-14 test body,
when read at the assertion block,
then it verifies that the `§ 3.2` comment appears within 5 lines of the `env: subEnv` expression (checking adjacency, not just substring presence anywhere in the section).

**AC-R36-10** (AC-R32-18 discriminating assertion)
Given `test/q32-slice3-close-walk.test.ts` AC-R32-18 test body,
when REVIEWER-REPORT-R32.md contains `**CRITICAL count: 0**` (the summary table line),
then the assertion does NOT trigger (it only fires on numbered finding headings like `/^#### CRITICAL-\d+/m` or `/^## CRITICAL$/m`); mutation test: removing the `count: 0` line from the report does NOT flip the assertion; adding a `#### CRITICAL-1` heading DOES flip it.

**AC-R36-11** (execSync → execFileSync in q25)
Given `test/q25-l0-contract.test.ts` at the git-diff invocation,
when the line is read,
then it uses `execFileSync('git', ['diff', BASELINE_SHA + '..' + CHORE_A_SHA, '--name-only'], {encoding:'utf8'})` array form with no shell string interpolation; no occurrence of `execSync` for git operations remains in the file.

**AC-R36-12** (execSync → execFileSync in q30)
Given `test/q30-nvlink-adapter.test.ts` at the git-diff invocation,
when the line is read,
then it uses `execFileSync('git', ['diff', ...]` array form with no shell string; no `execSync` for git operations remains.

**AC-R36-13** (R26 MINOR-2 impl alignment)
Given `engine/topology/common-mode-attribution.ts` event_ts aggregation loop (formerly lines 186–191),
when the same member_shard appears in the touches array more than once (a multi-touch scenario),
then earliest/latest event_ts is computed per distinct member_shard value (not globally across all touches); the module docstring at the top of the file accurately describes this per-distinct-member-shard semantics.

**AC-R36-14** (Q-R26-SPEC.md AC-R26-14 disambiguation)
Given `coordination/specs/Q-R26-SPEC.md` at the AC-R26-14 table row (~line 552),
when the row's Then cell is read,
then the original "exit code is 0" claim is marked as superseded (via `~~strikethrough~~` or `[R32-amended: …]` prefix), and the "exit code is 2 (TS2688/TS5107…)" amendment is unambiguous; a future reader cannot encounter two contradictory exit-code claims in the same cell.

**AC-R36-15** (q28 MINOR-3 — snap2 source_id/source_version)
Given `test/q28-slurm-adapter.test.ts` at the whitespace-only input sub-case (`parseSlurmTopologyConf('  \n\n   \t\n', META)` at ~line 166),
when the assertions are read,
then `assert.strictEqual(snap2.source_id, META.sourceId)` and `assert.strictEqual(snap2.source_version, META.sourceVersion)` are present alongside the existing `deepEqual(snap2.nodes, [])` and `deepEqual(snap2.edges, [])` assertions.

### Deliverable 3 — R34 carry-forwards

**AC-R36-16** (Q-R34-SPEC.md LS-3 window boundary reconciliation)
Given `coordination/specs/Q-R34-SPEC.md` at the section containing the window boundary claim (LS-3 pre-flag entry),
when the section is read after amendment,
then an `[R36-amended]` block explicitly reconciles the boundary claim with the actual implementation (inclusive vs exclusive endpoint or equivalent), citing the empirical observation that surfaced the discrepancy.

**AC-R36-17** (Q-R34-SPEC.md LS-4 `\Z` regex fix)
Given `coordination/specs/Q-R34-SPEC.md` §3.6 pseudocode containing `\Z` anchor,
when the section is read,
then `\Z` is replaced with `$` (JavaScript end-of-string anchor) and an amendment note explains the correction.

**AC-R36-18** (SPEC-AUTHORING-CHECKLIST.md — R34 MAJOR-1 closure)
Given `coordination/SPEC-AUTHORING-CHECKLIST.md` (new file),
when the file is read,
then it contains an Architect-authored checklist item for ALLOWED_REGEX carve-outs that explicitly names the operator-commit class (coordination tier durable artifacts: STAGED-FOR-*, WAVE-PLAN-*, WAVE-GATE-*, CLUSTER-HANDOFF files), instructing future spec authors to add regex carve-outs for these paths OR document the gap with a recommendation that operators land methodology commits before STATUS=READY.

**AC-R36-19** (CLAUDE-ARCHITECT.md R34 reinforcement appends)
Given `CLAUDE-ARCHITECT.md` at the REINFORCEMENTS section terminal block,
when the file is read,
then exactly 3 new REINFORCED 2026-05-18 lines appear as specified in `coordination/STAGED-FOR-PHASE-2-CLOSE.md` Item 5 § "For CLAUDE-ARCHITECT.md": spec-internal-contradiction sweep for algorithmic boundary clauses, JavaScript regex validation of spec pseudocode, and ALLOWED_SET operator-commit class carve-out.

**AC-R36-20** (CLAUDE-IMPLEMENTER.md R34 reinforcement appends — pre-MR-2)
Given `CLAUDE-IMPLEMENTER.md` with R34 reinforcements applied but before MR-2 consolidation,
when the reinforcement section is read,
then exactly 3 new REINFORCED 2026-05-18 lines appear as specified in STAGED Item 5 § "For CLAUDE-IMPLEMENTER.md": spec-vs-impl semantic conflict HALT, invalid-regex direct fix over content workaround, and full-suite count assertion by composition.

### Deliverable 5 — MR-2 consolidation

**AC-R36-21** (CLAUDE-IMPLEMENTER.md reinforcement entry count after MR-2)
Given `CLAUDE-IMPLEMENTER.md` after all 3 MR-2 passes (Pass 1 de-dup + Pass 2 thematic + Pass 3 promote),
when the number of distinct `# REINFORCED` block entries is counted,
then the count is ≤ 30 (target: 25–30 per scope block; all institutional knowledge preserved in composite or pointer form).

**AC-R36-22** (CLAUDE-COMMON.md Pass 3 promotions)
Given `CLAUDE-COMMON.md` after MR-2 Pass 3,
when the file is read for the 3 promoted universal patterns,
then it contains: line-citation cite-then-verify discipline, data-flow-not-syntax verification, and encode-actual-results-verbatim (all were Implementer-specific; now applicable to every role).

**AC-R36-23** (MR-2 self-application gate — Rule 5)
Given the consolidated `CLAUDE-IMPLEMENTER.md` at chore-A,
when the Implementer applies the Rule 5 self-audit (grep for `content.includes(`, `.length > 0`, `typeof x ===` in rule text; check trigger conditions discoverable under composite headings),
then no instance of the weak patterns the file itself prohibits exists in the file's rule text; all composite headings name their trigger conditions explicitly.

### Deliverable 1 — PHASE-2-CLOSE-WALK.md

**AC-R36-24** (PHASE-2-CLOSE-WALK.md required sections)
Given `coordination/PHASE-2-CLOSE-WALK.md`,
when the file is read,
then it contains all 7 required sections: § 1 Phase 2 scope summary (SLICE 1–4 deliverable table), § 2 Architectural-assessment retrospective (multi-cluster methodology + 6 rules + 14 friction surfaces), § 3 Phase 3 entry framing (TAGGED-FUTURE), § 4 ADR walk (Addition #25 D2+D5 + Addition #26 D4 RECONFIRMED), § 5 Memorial state stamp (REINFORCED counts + 6 cross-project rules + MR-2 post-consolidation state), § 6 Phase 1 freeze-hook activation coupling stamp (R34 activation confirmed), § 7 Cross-references (all Phase 2 spec/Reviewer-report/round-summary/handoff/gate artifact pointers).

**AC-R36-25** (A16 D4 RECONFIRMED at all emit sites)
Given all Phase 2 files that declare `correlational_not_causal`,
when each declaration is read,
then the type is the string literal `'true'` (not boolean; not a generic `boolean` field) at every emit site (engine/events/event-conditional-attribution.ts and any co-emit sites), consistent with Addition #26 D4 wire-format invariant; PHASE-2-CLOSE-WALK.md § 4 explicitly documents this RECONFIRMED finding.

### Deliverable 6 — PR-F7 hybrid Reviewer audit evidence

**AC-R36-26** (PR-F7 Cell 4 disposition)
Given `coordination/PHASE-2-CLOSE-WALK.md` § 4 or § 6 and/or `coordination/specs/Q-R36-SPEC.md`,
when the PR-F7 evidence matrix is read,
then Cell 4 (mixed-signal robustness: PSU fires alongside non-PSU cross-rack co-event; attribution discriminates correctly) has either: a Reviewer-verified AC in this round's AC table, OR an explicit disposition note with evidence pointer to `test/q-md-f4-common-mode-injection.test.ts` AC-R26-4 and confirmation that Cell 4 was independently verified at R26 Reviewer stage and is UNCHANGED through Phase 2.

### Deliverable 7 — ANCHOR-BACKFLOW-2026-05-18.md

**AC-R36-27** (ANCHOR-BACKFLOW-2026-05-18.md structure)
Given `coordination/ANCHOR-BACKFLOW-2026-05-18.md`,
when the file is read,
then it contains sections for all 4 subprocess-hang PR candidates (pre-emit grilling rule, pipeline watchdog, Bash-tool orphan reaping, test-isolation failure-mode in spec template), a Tailscale Phase 3 capability pointer (STAGED Item 4), and the Coordinator memorial graduation entry; each PR candidate section includes proposed prose or unified diff against `anchor/` canonical paths.

### Infrastructure

**AC-R36-28** (tsc exits 0 at chore-A SHA)
Given the project at chore-A SHA,
when `npx tsc -p tsconfig.test.json --noEmit` is run,
then exit code is 0 (encode actual observed exit code verbatim per Rule 1).

**AC-R36-29** (test count at chore-A SHA)
Given the project at chore-A SHA,
when `node --test test/q36-phase2-close-walk.test.js` is run (standalone, NOT full suite — subprocess-hang avoidance),
then all AC-R36-* tests in that file pass with 0 failures; per-file observed counts are recorded verbatim in NEXT-ROLE.md with arithmetic.

**AC-R36-30** (anti-scope allowed-set — round-start to chore-A)
Given chore-A SHA (SHA-A) and round-start SHA (the HEAD at R36 entry, `36ab019`),
when `git diff 36ab019..SHA-A --name-only` is run,
then every path in the diff matches at least one regex in the AC-R36-30 ALLOWED_SET (defined in `test/q36-phase2-close-walk.test.ts`):

```
/^test\/q36-phase2-close-walk\.test\.ts$/
/^test\/q29-k8s-adapter\.test\.ts$/
/^test\/q34-event-conditional-attribution\.test\.ts$/
/^test\/q32-slice3-close-walk\.test\.ts$/
/^test\/q25-l0-contract\.test\.ts$/
/^test\/q30-nvlink-adapter\.test\.ts$/
/^test\/q28-slurm-adapter\.test\.ts$/
/^engine\/topology\/common-mode-attribution\.ts$/
/^coordination\/SCOPING-MEMO-v0\.3\.md$/
/^coordination\/specs\/Q-R32-SPEC\.md$/
/^coordination\/specs\/Q-R26-SPEC\.md$/
/^coordination\/specs\/Q-R34-SPEC\.md$/
/^coordination\/specs\/Q-R36-SPEC\.md$/
/^coordination\/PHASE-2-CLOSE-WALK\.md$/
/^coordination\/ANCHOR-BACKFLOW-2026-05-18\.md$/
/^coordination\/SPEC-AUTHORING-CHECKLIST\.md$/
/^CLAUDE-ARCHITECT\.md$/
/^CLAUDE-IMPLEMENTER\.md$/
/^CLAUDE-COMMON\.md$/
/^coordination\/MEMORIAL\.md$/
/^coordination\/NEXT-ROLE\.md$/
/^coordination\/reviews\/REVIEWER-REPORT-R36\.md$/
```

**AC-R36-31** (chore-B forward protection)
Given chore-A SHA (SHA-A),
when a chore-B runtime test runs `execFileSync('git', ['diff', SHA_A + '..HEAD', '--name-only'], ...)`,
then every path in the output matches one of: `^coordination\/reviews\/REVIEWER-REPORT-R36\.md$`, `^coordination\/MEMORIAL\.md$`, `^coordination\/NEXT-ROLE\.md$` (only Reviewer/Memorial-Updater artifacts after chore-A).

---

## 4. Anti-scope

- **A12** frozen: engine internals unchanged EXCEPT `engine/topology/common-mode-attribution.ts` (R26 MINOR-2, pre-authorized)
- **A10/A11/A13/A16/A17** all preserved
- No Phase 3 entry work
- No modification of Wave 1–4 deliverables outside the pre-authorized list in § 2.2
- No modification of `cluster-scopes/wave-{1,2,3,4}/` (frozen)
- No modification of `CLUSTER-HANDOFF-*`, `WAVE-GATE-{01–04}.md`, `WAVE-PLAN-{01–03}.md` (frozen)
- No new vendor adapter code (TAGGED-FUTURE)
- No Tailscale infrastructure setup (STAGED Item 4, Phase 3 candidate)

---

## 5. Open Questions

None. All OQs resolved by overnight authority:
- OQ-W4-1 = A (MR-2 bundled here)
- OQ-W1-2 = A (audit + HYBRID_REVIEWER=true)
- OQ-W4-2 = A (Tailscale defers to Phase 3)
- OQ-W4-3 = A (anchor backflow bundled here)

---

## 6. Pre-emit grilling (required discipline)

1. ✅ Every Then-column field in each AC enumerated above — per Rule 3
2. ✅ AC-R36-30 ALLOWED_SET includes `coordination/reviews/REVIEWER-REPORT-R36.md` + `coordination/MEMORIAL.md` — per Rule 4
3. ✅ AC-R36-6 is a placement-aware assertion; mutation test explicitly described — per Rule 5 (rule-derivation-without-self-application: grep weak patterns in this spec's own AC assertions: none of the Then clauses use bare `includes(...)` without structural anchoring)
4. ✅ AC-R36-10 includes explicit mutation test for both false-positive and true-positive cases
5. ✅ AC-R36-26 provides explicit Cell 4 disposition (Reviewer-verified or evidence pointer) — per REINFORCED 2026-05-18 R32 MINOR-4
6. ✅ No `\Z` regex patterns in any spec pseudocode — per REINFORCED 2026-05-18 R34 MINOR-3 (CLAUDE-ARCHITECT.md)
7. ✅ Rule 6 self-application: any workaround during implementation must produce DIAGNOSTIC at point-of-encounter, not MEMORIAL-only

_Grilling result: spec is consistent and actionable. Proceeding to RED commit._
