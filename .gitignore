node_modules/
.DS_Store
*.log
*.bak
runs/
# TypeScript compiled output (generated by tsc -p tsconfig.test.json; co-located with .ts source)
*.js
*.js.map
# Library build output
dist/
.auto-memory/
.env
.env.local
dist/
build/
coverage/
*.tsbuildinfo

# Anchor Mode 2 pipeline artifacts (gitignored per new-project.sh convention)
coordination/.prompt-*.md
coordination/.pipeline-*.lock
coordination/.role-stamp
coordination/clusters/
coordination/multi-track-status.json

# R72: exempt coordination/coverage/ from the root-level coverage/ gitignore rule
# (git's 'coverage/' pattern without leading slash matches all dirs named coverage/ in tree)
!coordination/coverage/
