node_modules/
.DS_Store
*.log
*.bak
runs/
# TypeScript compiled output (generated by tsc -p tsconfig.test.json; co-located with .ts source)
*.js
*.js.map
# Library build output
dist/
.auto-memory/
.env
.env.local
dist/
build/
coverage/
*.tsbuildinfo

# R82: engine browser-bundle (build output of `pnpm build:browser`)
demos/engine-bundle.mjs

# Anchor pipeline working state. coordination/ is the orchestrator's LOCAL working directory
# (PRD, role directives, memorials, specs, reviews, round logs) — not part of the published product:
# no src/ or test/ code depends on it (product tests read only src/ + fixtures + coverage-matrices/).
# Kept locally for run-pipeline.sh (override location with --coord-dir / ANCHOR_COORD_DIR); not tracked.
coordination/

# clustersynth perf bench: per-run reports (timestamped, regenerable)
bench/results/

# clustersynth fixtures dropped into _substrate for bench/smoke tests
test/_substrate/clustersynth-*.json
