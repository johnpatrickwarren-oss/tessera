# pnpm configuration (per R68 migration; project-wide convention to avoid
# npm supply-chain attacks via content-addressable store + strict-peer-deps +
# workspace-scoped resolution)
auto-install-peers=true
strict-peer-dependencies=true
prefer-frozen-lockfile=true
